server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/byggfirma_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: byggfirma
    password: byggfirma123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

app:
  admin:
    username: admin
    password: admin123
